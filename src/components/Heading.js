import React from "react";
import styled from "styled-components";

const Heading = () => {
  return <HeadingWrapper>This is Heading</HeadingWrapper>;
};

const HeadingWrapper = styled.div`
  color: pink;
`;

export default Heading;
