import React from "react";
import styled from "styled-components";

const Content = () => {
  return <ContentWrapper>This is Content</ContentWrapper>;
};

const ContentWrapper = styled.div`
  color: pink;
`;

export default Content;
